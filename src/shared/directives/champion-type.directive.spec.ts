import { ChampionTypeDirective } from './champion-type.directive';

describe('ChampionTypeDirective', () => {
  it('should create an instance', () => {
    const directive = new ChampionTypeDirective();
    expect(directive).toBeTruthy();
  });
});
